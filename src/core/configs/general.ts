const parvinConfig = {
    templateVariableSyntax: '$$',
    templateVariableSyntaxRegex: /\$\$([\w.]+)/g,
    templateEventSyntax: '$',
    ifConditionName: 'if',
    elseIfConditionName: 'else-if',
    elseConditionName: 'else',
    templateModelBindingSyntax: 'model',
}

export default parvinConfig
